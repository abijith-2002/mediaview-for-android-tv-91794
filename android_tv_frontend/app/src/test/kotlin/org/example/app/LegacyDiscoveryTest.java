package org.example.app;

import org.junit.Test;
import static org.junit.Assert.assertTrue;

/**
 * PUBLIC_INTERFACE
 * LegacyDiscoveryTest
 * Ensures at least one JUnit4-compatible test exists so Gradle's default unit test discovery finds tests.
 */
public class LegacyDiscoveryTest {

    @Test
    public void testLegacyDiscoveryPasses() {
        assertTrue(true);
    }
}
